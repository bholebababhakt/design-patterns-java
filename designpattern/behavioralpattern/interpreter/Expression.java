package designpattern.behavioralpattern.interpreter;

//Abstract Expression: An interface or abstract class that defines the common method(s) for interpreting an expression.
interface Expression {
    int interpret();
}
